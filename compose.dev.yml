services:
  app:
    image: node:24
    user: "${UID:-1000}:${GID:-1000}"
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "${APP_PORT:-3001}:${APP_PORT:-3001}"
    command: sh -c "npm install && PORT=${APP_PORT:-3001} npm run dev"
